name: flutter_causality
description: "Flutter extensions for the causality Dart package. Easily observe causes in your widget tree."
repository: https://github.com/Goddchen/causality/tree/main/packages/flutter_causality
version: 0.0.1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  causality: ^0.0.1
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter
  very_good_analysis: ^5.1.0
